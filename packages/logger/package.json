{
  "name": "@google-cloud/common-logger",
  "version": "0.1.0",
  "description": "A shared logger for Google node.js modules.",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "scripts": {
    "test": "npm run test-only",
    "codecov": "nyc report --reporter=json && codecov -f coverage/*.json",
    "test-only": "nyc mocha build/test",
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc -p .",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check"
  },
  "keywords": [],
  "files": [
    "build/src"
  ],
  "author": "Google Inc.",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/mocha": "^5.2.4",
    "@types/node": "^10.5.2",
    "@types/shimmer": "^1.0.1",
    "@types/sinon": "^5.0.1",
    "codecov": "^3.0.4",
    "gts": "^0.7.1",
    "hard-rejection": "^1.0.0",
    "mocha": "^5.2.0",
    "nyc": "^12.0.2",
    "shimmer": "^1.2.0",
    "sinon": "^6.1.3",
    "source-map-support": "^0.5.6",
    "typescript": "^2.9.0"
  },
  "nyc": {
    "exclude": [
      "build/test"
    ]
  }
}
